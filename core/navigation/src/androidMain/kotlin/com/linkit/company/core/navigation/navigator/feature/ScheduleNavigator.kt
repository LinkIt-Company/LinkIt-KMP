package com.linkit.company.core.navigation.navigator.feature

import com.linkit.company.core.navigation.navigator.Navigator

/**
 * ScheduleActivity로의 전환을 담당하는 Navigator.
 */
interface ScheduleNavigator : Navigator
